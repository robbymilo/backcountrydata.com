---
kind: pipeline
type: docker
name: website

platform:
  os: linux
  arch: amd64

steps:
  - name: docker  
    image: plugins/docker
    settings:
      repo: robbymilo/backcountrydata
      dockerfile: api/Dockerfile

trigger:
  event:
  - push
